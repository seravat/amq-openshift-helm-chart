{{- if .Values.operator }}
{{- if eq .Values.operator.scope "cluster" }}
---
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: {{ print .Values.amq.name "-og" }}
spec:
  targetNamespaces:
    - {{ .Release.Namespace }}
{{- end }}
{{- end }}